version: 2
models:
- name: int__sid_chgs
  description:  '{{ doc("int__sid_chgs_description") }}' 
  config:
    tags:
    - IP
    - SID
    - CHGS
    - dbt_int
  columns:
  - name: KEY
    type: double
    description: HCUP record identifier
  - name: CHARGE
    type: double
    description: Line item charges (as received from source)